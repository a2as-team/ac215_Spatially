networks:
    default:
        name: zoning-ordinance-rag-network
        external: true
services:
    zoning-ordinance-rag-cli:
        image: zoning-ordinance-rag-cli
        container_name: zoning-ordinance-rag-cli
        volumes:
            - ../secrets:/secrets
            - ../../:/app
        environment:
            GOOGLE_APPLICATION_CREDENTIALS: $GOOGLE_APPLICATION_CREDENTIALS
            GCP_PROJECT: $GCP_PROJECT
        depends_on:
            - chromadb
    chromadb:
        image: chromadb/chroma:latest
        container_name: zoning-ordinance-rag-chromadb
        ports:
            - 8001:8000
        volumes:
            - ./docker-volumes/chromadb:/chroma/chroma
        environment:
            - IS_PERSISTENT=TRUE
            - ANONYMIZED_TELEMETRY=FALSE
            - CHROMA_CORS_ALLOW_ORIGINS=["*"] # This is not recommended for production environments.
